[project]
name = "weatherterm"
version = "0.1.0"
description = "A simple weather dashboard for terminal interface"
readme = "README.md"
requires-python = ">=3.15"
dependencies = [
    "aiohttp>=3.14.3",
    "asyncio>=4.0.0",
    "requests>=2.34.2",
    "textual>=8.2.8",
]
